import { TwitterHoverDirective } from './twitter-hover.directive';

describe('TwitterHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new TwitterHoverDirective();
    expect(directive).toBeTruthy();
  });
});
